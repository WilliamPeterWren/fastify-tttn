const addToCart = require('./add-to-cart.schema');

const getAll = require('./get-all.schema');
const update = require('./update.schema');
const remove = require('./remove.schema');

module.exports = {
  addToCart,
  update,
  getAll,
  remove
}