const register = require('./register.staff.schema');
const getStaff = require('./get-staff.schema');
const updateRole = require('./update-role.schema');

module.exports = {
  register,
  updateRole,
  getStaff
}