const purchase = require('./purchase.schema');
const getAll = require('./get-all.schema');
const getOne = require('./get-one.schema');
const update = require('./update.schema');

module.exports = {
  purchase,
  getAll,
  update,
  getOne,
}